<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= movie.title %></title>
    <link rel="stylesheet" href="/css/styles.css" />
    <style>

      html, body {
        height: 100%;
        margin: 0;
        display: flex;
        flex-direction: column;
      }

      .movie-detail-container {
        flex: 1; /* ✅ ocupa todo el espacio disponible antes del footer */
        max-width: 1400px;
        margin: 0 auto;
        padding: 5% 10%;
      }

      .movie-hero {
        background: linear-gradient(135deg, #2a2a2a 0%, #1a1a1a 100%);
        border: 3px solid #ffd700;
        border-radius: 20px;
        padding: 40px;
        margin-bottom: 40px;
        box-shadow: 0 10px 40px rgba(255, 215, 0, 0.2);
      }

      .movie-hero h1 {
        font-size: 3.5rem;
        color: #ffd700;
        margin: 0 0 10px 0;
        font-weight: 800;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
      }

      .movie-hero .movie-date {
        font-size: 1.2rem;
        color: #999;
        margin: 0;
      }

      .info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 30px;
        margin-bottom: 20px;
      }

      .info-card {
        background: #2a2a2a;
        border: 2px solid #444;
        border-radius: 15px;
        padding: 30px;
        transition: all 0.3s ease;
      }

      .info-card:hover {
        border-color: #ffd700;
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(255, 215, 0, 0.3);
      }

      .info-card h2 {
        color: #ffd700;
        font-size: 1.8rem;
        margin: 0 0 20px 0;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .info-card p {
        color: #ddd;
        line-height: 1.8;
        margin: 10px 0;
      }

      .info-card strong {
        color: #ffd700;
      }

      .info-card ul {
        list-style: none;
        padding: 0;
        margin: 0;
      }

      .info-card li {
        color: #fff;
        padding: 8px 0;
        border-bottom: 1px solid #333;
        font-weight: 400;
      }

      .info-card li:last-child {
        border-bottom: none;
      }

      .info-card a {
        color: #fff;
        text-decoration: none;
        transition: color 0.3s ease;
      }

      .info-card a:hover {
        color: #ffd700;
      }

      .synopsis-card {
        grid-column: 1 / -1;
        background: linear-gradient(135deg, #2a2a2a 0%, #1f1f1f 100%);
      }

      .general-info-card {
        grid-column: 1 / -1;
      }

      .general-info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-top: 10px;
      }

      .info-item {
        background: #1a1a1a;
        padding: 15px;
        border-radius: 10px;
        border-left: 4px solid #ffd700;
      }

      .info-item p {
        color: #fff;
      }

      .cast-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 20px;
        margin-top: 20px;
      }

      .actor-card {
        background: #1a1a1a;
        padding: 20px;
        border-radius: 12px;
        border: 2px solid #333;
        transition: all 0.3s ease;
        text-align: center;
      }

      .actor-card:hover {
        border-color: #ffd700;
        transform: scale(1.05);
      }

      .actor-card strong {
        display: block;
        color: #fff;
        font-size: 1.1rem;
        margin-bottom: 8px;
        font-weight: 400;
      }

      .actor-card .character {
        color: #999;
        font-size: 0.9rem;
        margin: 5px 0 0 0;
      }

      .cast-section {
        grid-column: 1 / -1;
      }

      .crew-section {
        grid-column: 1 / -1;
      }

      .crew-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 15px;
        margin-top: 20px;
      }

      .crew-list li {
        background: #1a1a1a;
        padding: 15px;
        border-radius: 8px;
        border-left: 3px solid #ffd700;
      }

      .back-link {
        display: inline-block;
        background: #ffd700;
        color: #1a1a1a;
        padding: 15px 40px;
        border-radius: 30px;
        text-decoration: none;
        font-weight: 700;
        font-size: 1.1rem;
        transition: all 0.3s ease;
        margin-top: 20px;
      }

      .back-link:hover {
        background: #fff;
        transform: scale(1.05);
        box-shadow: 0 5px 20px rgba(255, 215, 0, 0.4);
      }

      @media (max-width: 768px) {
        .movie-hero h1 {
          font-size: 2rem;
        }

        .info-grid {
          grid-template-columns: 1fr;
        }

        .cast-grid {
          grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
        }
      }
    </style>
  </head>
  <body>
    <%- include('common/navbar.ejs', { toSearch: movie.title }) -%>
    <div class="movie-detail-container">
      <div class="movie-hero">
        <h1><%= movie.title %></h1>
        <% if (movie.release_date) { const monthNames = ["Ene", "Feb", "Mar",
        "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic"]; const
        date = new Date(movie.release_date); const formattedDate =
        `${date.getDate()}-${monthNames[date.getMonth()]}-${date.getFullYear()}`;
        %>
        <p class="movie-date"><%= formattedDate %></p>
        <% } %>
      </div>

      <div class="info-grid">
        <% if (movie.directors && movie.directors.length) { %>
        <div class="info-card">
          <h2>Dirección</h2>

          <ul>
            <% movie.directors.forEach(director => { %>
            <li>
              <a href="/director/<%= director.crew_member_id %>"
                ><%= director.crew_member_name %></a
              >
            </li>
            <% }) %>
          </ul>
        </div>
        <% } %> <% if (movie.writers && movie.writers.length) { %>
        <div class="info-card">
          <h2>Guion</h2>
          <ul>
            <% movie.writers.forEach(writer => { %>
            <li><%= writer.crew_member_name %></li>
            <% }) %>
          </ul>
        </div>
        <% } %> <% if (movie.overview) { %>
        <div class="info-card synopsis-card">
          <h2>Sinopsis</h2>
          <p><%= movie.overview %></p>
        </div>
        <% } %> <% if ( (movie.countries && movie.countries.length) ||
        (movie.genres && movie.genres.length) || movie.originalLanguage ||
        (movie.productionCompanies && movie.productionCompanies.length) ) { %>
        <div class="info-card general-info-card">
          <h2>Información General</h2>
          <div class="general-info-grid">
            <% if (movie.countries && movie.countries.length) { %>
            <div class="info-item">
              <strong> Países</strong>
              <p><%= movie.countries.join(', ') %></p>
            </div>
            <% } %> <% if (movie.genres && movie.genres.length) { %>
            <div class="info-item">
              <strong> Géneros</strong>
              <p><%= movie.genres.join(', ') %></p>
            </div>
            <% } %> <% if (movie.originalLanguage) { %>
            <div class="info-item">
              <strong> Idioma</strong>
              <p><%= movie.originalLanguage %></p>
            </div>
            <% } %> <% if (movie.productionCompanies &&
            movie.productionCompanies.length) { %>
            <div class="info-item">
              <strong> Productoras</strong>
              <p><%= movie.productionCompanies.join(', ') %></p>
            </div>
            <% } %>
          </div>
        </div>
        <% } %> <% if (movie.cast && movie.cast.length) { %>
        <div class="info-card cast-section">
          <h2>Elenco Principal</h2>
          <div class="cast-grid">
            <% movie.cast.sort((a,b) => a.cast_order - b.cast_order).slice(0,
            12).forEach(actor => { %>
            <a href="/actor/<%= actor.actor_id %>" class="actor-card"><%= actor.actor_name %>
              <span
                style="
                  display: block;
                  color: #fff;
                  font-size: 1.1rem;
                  margin-bottom: 8px;
                  font-weight: 400;
                "
                ></span>

              <% if (actor.character_name) { %>
              <p class="character"><%= actor.character_name %></p>
              <% } %>
              </a>
            <% }) %>
          </div>
        </div>
        <% } %> <% if (movie.crew && movie.crew.length) { %>
        <div class="info-card crew-section">
          <h2>Equipo Técnico</h2>
          <ul class="crew-list">
            <% movie.crew.slice(0, 20).forEach(member => { %>
            <li>
              <span style="font-weight: 400"
                ><%= member.crew_member_name %></span
              >
              — <%= member.job %>
            </li>
            <% }) %>
          </ul>
        </div>
        <% } %>
        </div>
        <div style="text-align: center">
          <a href="/" class="back-link"> Volver al inicio</a>
        </div>
    </div>

        <%- include('common/footer.ejs') -%>


  </body>
</html>
